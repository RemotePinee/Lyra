{
  "name": "@deepwise/agent-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The agent that works on this repository, run from CI.",
  "bin": { "deepwise-agent": "./src/main.ts" },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "@deepwise/core": "workspace:*"
  }
}
